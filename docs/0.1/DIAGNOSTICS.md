# Diagnostics and failure injection

Implemented:

- bounded structured diagnostic events;
- component health snapshots;
- short validated diagnostic codes;
- details that reject common secret-field patterns and control characters;
- deterministic JSON export without message bodies, keys or capability values;
- bounded ring-buffer retention;
- deterministic fail-point registry for storage, relay and transport integration tests.
