name: torca_avatar
description: Asynchronous deterministic avatar rendering and persistent cache for Torca.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  avatar_genome:
    path: ../../third_party/avatar_genome
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  torca_ui:
    path: ../torca_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
