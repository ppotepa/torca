# Torca 0.1 progress and handoff

> Canonical source/validation status for Torca 0.1.

Last updated: **2026-08-07**  
Active branch: **`main`**  
Target: **Torca 0.1 alpha**  
Core source phase: **complete**  
UI productization phase: **active — UI-02 next**  
Validation phase: **owner/platform/end-to-end validation still required**

## Core source status

The original 20 dependency-ordered source batches are complete. The production path remains:

```text
Flutter -> EngineGateway -> Bridge -> torca-native -> NativeEngineRuntime
  -> ClientEngineActor -> RuntimeHost
     -> PairingDriver / OwnedTorDriver / TorcaCommunicationDriver
```

Rust/runtime owns Tor, pairing, peer sessions, durable delivery/retry, receipts, persistence, cryptography, relationship administration, attachment transfer/export and diagnostics data. Flutter owns presentation and platform UI integration only.

Current core gates remain source-complete but owner validation is open for Windows, Android, real Tor/two-client messaging, restart/reconnect, SQLCipher/protected secrets, attachments, notifications/deep links and release packaging. See [`docs/0.1/RELEASE_HANDOFF.md`](docs/0.1/RELEASE_HANDOFF.md) and [`docs/0.1/TEST_MATRIX.md`](docs/0.1/TEST_MATRIX.md).

Bridge contract before the PeerHealth slice remains **v9**. Pairing protocol remains **v2**. SQLCipher migration catalog remains **1–17**.

## Dependency/reproducibility status

`Cargo.lock` is reconciled. Flutter `pubspec.lock` still requires the first owner `flutter pub get`, inspection and accepted commit because this implementation environment has no Flutter/Dart SDK.

## Torca 0.1 UI Productization

Plan: [`docs/0.1/UI_IMPLEMENTATION_ORDER.md`](docs/0.1/UI_IMPLEMENTATION_ORDER.md)

| Batch | Scope | Status |
|---|---|---|
| UI-00 | Plan + tracking | DONE |
| UI-01 | Boundary + preferences | SOURCE DONE / VALIDATION OPEN |
| UI-02 | Theme system | NOT STARTED |
| UI-03 | Settings + app shell | NOT STARTED |
| UI-04 | Connection primitives | NOT STARTED |
| UI-05 | PeerHealth backend | NOT STARTED |
| UI-06 | Bridge v10 | NOT STARTED |
| UI-07 | Connection UI/header | NOT STARTED |
| UI-08 | Message bubbles | NOT STARTED |
| UI-09 | Context/actions | NOT STARTED |
| UI-10 | Attachments | NOT STARTED |
| UI-11 | Pairing polish | NOT STARTED |
| UI-12 | Diagnostics | NOT STARTED |
| UI-13 | Notifications/tray | NOT STARTED |
| UI-14 | Desktop/accessibility | NOT STARTED |
| UI-15 | Final audit | NOT STARTED |

Current UI batch: **UI-02 — Theme system**.

### UI-00 evidence

- UI implementation order and ownership rules committed;
- original core completion preserved;
- canonical UI tracker registered.

### UI-01 evidence

- `LocalPreferences` is created and loaded by `main.dart`, then injected into `TorcaApp`;
- preferences are limited to persisted `themeMode` and `notificationsEnabled` for 0.1;
- notification message preview preference removed;
- temporary Open-export cleanup removed from Flutter preferences;
- controlled temporary export cleanup now runs best-effort inside the verified Rust attachment export boundary and never scans arbitrary Save As filenames;
- Flutter tests now construct the real app composition contract instead of relying on stale constructor usage;
- stale `bridge v4` test wording removed.

UI-01 owner validation still required: run `flutter pub get`, `flutter analyze`, `flutter test`, and validate preference persistence plus attachment Open/cleanup behavior on Windows and Android.

## Tracked presentation-boundary debt

Current v9 bridge commands still accept some entity IDs and timestamps generated by Flutter (`identityId`, pairing session IDs, message/attachment IDs and selected operation timestamps). This is explicitly tracked and is not a blocker for UI-02. Revisit when a bridge contract change is already justified rather than performing a speculative rewrite.

## Release gates still open

- first accepted Flutter lockfile;
- clean Windows and Android builds;
- real packaged Tor/onion validation;
- two-device pairing/direct messaging/reconnect/receipts/exactly-once;
- attachment interruption/resume/Open/Save As;
- Windows lifecycle/tray/deep links/notifications;
- Android foreground lifecycle/deep links/notifications;
- signing/release matrix.
