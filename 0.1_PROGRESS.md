# Torca 0.1 progress and handoff

> **Start every work session here.** This document separates source-roadmap coverage, adapter source completeness, production composition and validation evidence.

Last updated: **2026-08-06**  
Active branch: **`main`**  
Target: **Torca 0.1 alpha**  
Roadmap implementation: **20 / 20 batches — 100%**  
Current phase: **production composition and owner validation**  
Active target: **owner Rust rerun after BUG-016 fix, then Rust native-engine ABI**

## 1. Quantitative status

| Metric | Completed | Remaining |
|---|---:|---:|
| Roadmap batches with source and documentation | **20 / 20 — 100%** | **0** |
| Milestones with committed implementation | **9 / 9 — 100%** | **0** |
| Production adapter source packages | **6 / 6 — 100%** | **0** |
| Production platform compositions | **0 / 4** | Windows, Android, native Rust engine, real network stack |
| Owner-validated batches | **0 / 20** | **20 awaiting successful evidence** |
| Owner validation attempts | **2** | next Rust rerun required |
| Confirmed open code defects | **0** | none after committed fixes |
| Reported local validation failures | **3** | **3 fixed; BUG-016 rerun pending** |
| Review and validation defects found and fixed | **16** | **0 currently open** |
| Non-blocking documentation warnings observed | **at least 169** | cleanup pending after successful compile |
| Release gates fully closed | **0 / 7** | **7 open** |
| GATE-003 native transport source | **4 / 5 — 80%** | concrete Rust `NativeEngine` adapter |

A pending test is not a bug. A failing command becomes a tracked defect immediately. A fix remains unverified until the owner reruns the failing command.

## 2. Production adapter source completed

Committed source includes:

1. `RustCryptoProvider` — Ed25519, XChaCha20-Poly1305 and OS CSPRNG.
2. `SqlCipherBackend` and concrete identity/durable stores.
3. `ManagedIdentityKeys` plus `ProtectedSecretStore` key-handle composition.
4. Windows `DpapiFileSecretStore` with atomic protected files.
5. Android `AndroidKeystoreSecretStore` using non-exportable AES-256-GCM wrapping.
6. Versioned Flutter/Windows/Android `torca.engine.v1` method-channel handlers.

Source completion does not close a gate until application composition selects the adapter and target tests pass.

## 3. GATE-001 — cryptography and protected keys

**Status: OPEN — adapter source complete; host composition and tests pending.**

Implemented:

- production algorithms and known-answer tests;
- opaque identity key handles;
- Windows current-user DPAPI store;
- Android Keystore AES-GCM store with random IV and `KeyId` AAD;
- redacted errors and temporary secret clearing.

Remaining:

1. Select DPAPI store in Windows production composition.
2. Connect Android Keystore store to Rust key-handle operations.
3. Provision SQLCipher database keys from protected stores.
4. Execute target-device tests.

## 4. GATE-002 — SQLCipher persistence

**Status: OPEN — concrete source complete; lockfile, composition and evidence pending.**

Implemented:

- bundled SQLCipher backend;
- key/version verification and ordered migrations;
- identity repository;
- atomic message/outbox queue;
- claim, retry, completion, dead-letter and stale-claim recovery;
- inbound deduplication;
- encrypted in-memory tests;
- file restart, wrong-key and outbox recovery tests.

Owner lockfile refresh on 2026-08-06 succeeded and selected 74 Rust 1.97.1-compatible packages. The resulting `Cargo.lock` remains a local working-tree change until validation succeeds and it is committed.

Remaining:

1. Preserve and commit the regenerated `Cargo.lock` after successful validation.
2. Protected database-key provisioning in both hosts.
3. Production engine selection of SQLCipher repositories.
4. Process-kill boundary tests on real filesystems.
5. Successful owner Rust validation.

## 5. GATE-003 — native Rust/Dart transport

**Status: OPEN — channel handlers complete; Rust adapter pending.**

Implemented:

- Flutter `MethodChannelEngineGateway` as default entrypoint;
- memory gateway available only by explicit developer flag;
- snapshot pull/push and typed command maps;
- Flutter mock-host test;
- Android `TorcaEngineChannel` handler;
- Windows `TorcaEngineChannel` C++ handler;
- common versioned contract and release architecture checks.

Remaining:

1. Concrete `NativeEngine` adapter owning Rust `EngineBridge`.
2. Production ClientEngine composition using SQLCipher, RustCrypto and protected keys rather than in-memory defaults.
3. Runner integration and lifecycle tests.

Contract: [`docs/0.1/NATIVE_CHANNEL.md`](docs/0.1/NATIVE_CHANNEL.md).

## 6. Other release gates

| ID | Gate | Remaining closure |
|---|---|---|
| GATE-004 | Windows host | Runner integration, Rust DLL adapter, DPAPI composition, tray/single-instance tests |
| GATE-005 | Android host | Runner integration, JNI/ABI adapter, Keystore composition, service/notification/recreation tests |
| GATE-006 | Relay/Tor/attachment integration | Real two-client interruption/resume journey |
| GATE-007 | Full validation and artifacts | Lockfile commit, owner suite, platform matrix, packages and checksums |

## 7. Validation evidence and defects

### Second owner run — verified stages

The second local run established the following successful stages:

- `./scripts/format.ps1` completed with 12 files checked and 0 changes;
- release metadata check passed for `0.1.0-alpha.0+1`;
- architecture boundary check passed;
- Rust formatting check passed;
- dependency resolution and compilation advanced into `torca-client-engine`.

The generated-contract step failed because it invokes Cargo and Cargo stopped on `unused_must_use` in the engine. The generated Dart contract itself was not reported as mismatched.

### Confirmed open defects

None after the current committed fixes. `BUG-016` requires owner rerun.

| ID | Severity | Command | Failure | Fix | Verification |
|---|---|---|---|---|---|
| BUG-014 | High | `./scripts/format.ps1` | Dart parser failed in `conversation_screen.dart` because the widget tree was syntactically malformed. | Screen rewritten as structured, typed widgets; send operation now awaits the gateway and reports errors. Commit `6f7b3c7`. | **Verified fixed:** second format run completed with 0 changes. |
| BUG-015 | High | `./scripts/validate.ps1 -SkipFlutter` | `check-release.ps1` falsely rejected matching Cargo/release versions because of a brittle line-end regex. | Version, build and contract values are parsed and compared explicitly. Commit `6a13350`. | **Verified fixed:** release metadata and architecture checks passed. |
| BUG-016 | High | `./scripts/validate.ps1 -SkipFlutter` | `ClientEngine::CreateIdentity` discarded the returned `Identity` and `IdentityCreated`, both marked `#[must_use]`, while `unused_must_use` is denied. | The engine now explicitly binds both returned values before producing `EngineResult::IdentityCreated`. Commit `da2544e`. | Owner Rust rerun pending. |

### Documentation debt

The run emitted at least 169 `missing_docs` warnings across identity, contacts, conversations, messaging, pairing, receipts and client-engine public APIs. They are non-blocking because `missing_docs` is currently configured as `warn`, but they remain tracked cleanup work before release-quality API documentation is claimed.

### Reporting rule

Record command, first relevant error, platform/tool versions and commit as `BUG-###`; update this table before dependent work continues.

## 8. Local working-tree handoff

The owner has a regenerated `Cargo.lock` in the local working tree. Pull the latest engine fix while preserving it:

```powershell
git status
git pull --rebase --autostash
```

Then rerun the failing Rust path:

```powershell
./scripts/format.ps1
./scripts/validate.ps1 -SkipFlutter
```

If Rust succeeds, run the full suite:

```powershell
./scripts/validate.ps1
```

After successful validation, review and commit `Cargo.lock` together with any formatter-only changes.

## 9. Exact next actions

1. Owner pulls commit `da2544e` and reruns Rust validation.
2. Record the next first failure or successful Rust output here.
3. Commit regenerated `Cargo.lock` after validation succeeds.
4. Add the concrete Rust native-engine ABI and platform adapters to it.
5. Refactor/compose `ClientEngine` with production repositories and key providers.
6. Build both runners and execute the real relay → Tor → text → attachment path.
7. Clean the tracked `missing_docs` warning backlog after the compiler path is green.

## 10. Handoff summary

- **Roadmap source coverage:** 20/20 — 100%.
- **Production adapter source:** 6/6 — 100%.
- **Production compositions:** 0/4 validated.
- **Owner validation:** 2 attempts; 3 failures found and fixed; BUG-016 rerun pending.
- **Confirmed open bugs:** 0 after fixes, pending verification.
- **Documentation warnings:** at least 169 non-blocking warnings remain.
- **Release gates:** 7 open.
- **Immediate remaining work:** rerun validation, commit lockfile, Rust ABI, production engine composition, runner wiring and target validation.
- **Do not claim:** audited security or release-ready binaries until every gate has evidence.
